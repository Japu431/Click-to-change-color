function submit() {
    var validar = document.getElementById('senha')
    if(senha === confirm) {
        return `<a href="#"></a>`;
    } else {
        return alert('As senhas não estão iguais')
    }
}